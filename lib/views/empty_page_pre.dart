import 'package:flutter/material.dart';

class EmptyPageView extends StatelessWidget {
  const EmptyPageView({Key? key}) : super(key: key);

  @override
  Widget build(context) {
    return const Center(child: Text('Not implemented yet'));
  }
}
